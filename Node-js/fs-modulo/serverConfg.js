const serverConfig = {
  port: 3000,
  host: "localhost",
  database: {
    host: "localhost",
    username: "root",
    password: "password",
    database: "mydatabase",
  },
};

module.exports = serverConfig;
